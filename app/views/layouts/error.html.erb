<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
<head>
	<meta charset="utf-8" />
  <meta name="apple-mobile-web-app-title" content="uberZeit">
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= "#{@status_code} #{@rescue_response.to_s.titleize}" %></title>
  <link rel="fa_icon" href="<%= ENV['RAILS_RELATIVE_URL_ROOT'] %>/favfa_icon.ico" type="image/x-fa_icon">
  <link rel="shortcut fa_icon" href="<%= ENV['RAILS_RELATIVE_URL_ROOT'] %>/favfa_icon.ico" type="image/x-fa_icon">
	<%= stylesheet_link_tag    "application" %>
	<%= javascript_include_tag "vendor/custom.modernizr" %>
  <%= javascript_include_tag "application" %>
</head>
<body class="content-<%= controller.controller_name %>">
  <%= render 'layouts/header' %>
  <div class="content container">
    <div class="row">
      <div class="twelve columns">
        <h4><%= "#{@status_code} #{@rescue_response.to_s.titleize}" %></h4>
        <div class="alert-box alert">
          <%= yield %>
        </div>
      </div>
    </div>
  </div>
  <%= render 'layouts/footer' %>
</body>
</html>
